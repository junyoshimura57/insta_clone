/ アクション名のビューファイルが呼ばれる。
h2 = "#{@user_to.username}さん"
p = "#{@user_from.username}さんがあなたの投稿にコメントしました。"
/ 絶対パスを表示するため_pathでなく、_urlを使用して投稿詳細画面のurlを生成する(アンカーリンク付き)。
= link_to "確認する", post_url(@comment.post, { anchor: "comment-#{@comment.id}"})
